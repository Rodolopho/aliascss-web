{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "acss-build": "aliascss --config",
        "acss-watch": "aliascss --config --watch"
    },
    "devDependencies": {
        "aliascss": "^1.0.9",
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    },
    "dependencies": {
        "@emmetio/codemirror-plugin": "^1.2.4"
    }
}
